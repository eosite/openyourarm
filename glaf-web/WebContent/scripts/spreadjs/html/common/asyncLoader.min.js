var GcSpread;
(function (n) {
	(function (n) {
		(function (n) {
			(function (t) {
				"use strict";
				function u(t) {
					r || (ko.applyBindings({
							res : n.res
						}, document.body), ko.applyBindings({
							res : n.res
						}, document.head));
					for (var u = 0; u < i.length; u++)
						i[u]();
					i = [],
					r ? (t.find(".loading-placeholder").addClass("hidden"), app.notifyReady()) : setTimeout(function () {
						t.find(".loading-placeholder").addClass("hidden"),
						app.notifyReady()
					}, 0),
					r = !0
				}
				function o(n) {
					i.push(n)
				}
				function e() {
					if (!t.rootFolder)
						return "./";
					var n = t.rootFolder.trim();
					return n.slice(-1) !== "/" && (n = n + "/"),
					n
				}
				function f(t) {
					t === undefined && (t = $(document));
					var f = e(),
					o = t.find("[data-include]"),
					r = [],
					i = [],
					h = [],
					s;
					return o.each(function () {
						var t = $(this),
						e = t.data("include"),
						o = t.data("includeScript"),
						s = t.data("includeCss"),
						u = f + e + "/" + e;
						s && n.util.loadCss(u + ".css"),
						r.push(n.util.loadHtml(u + ".html", "." + e, t)),
						o && i.push(u)
					}),
					$.when.apply($, r).then(function () {
						if (i.length !== 0) {
							var r = i.map(function (t) {
									return n.util.loadScript(t + ".js")
								});
							$.when.apply($, r).then(function () {
								u(t)
							})
						} else
							u(t)
					})
				}
				t.rootFolder = "../";
				var i = [],
				r;
				t.ready = o,
				t.loadContent = f
			})(n.loader || (n.loader = {}));
			var t = n.loader
		})(n.designer || (n.designer = {}));
		var t = n.designer
	})(n.Sheets || (n.Sheets = {}));
	var t = n.Sheets
})(GcSpread || (GcSpread = {}))
