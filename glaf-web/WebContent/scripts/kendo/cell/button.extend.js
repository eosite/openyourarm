function buttonCell(rule) {
	if (rule) {
		//事件更改到全局注册
		/*var $target = $(this._containerDiv).parents("[data-role=cell]"),
			eventsRule = $target.data("eventsRule");
		if (eventsRule) {
			var event = eventsRule[rule.id];
			if (event) {
				//$SC(rule, this).bind('click', event);
			}
		}*/
	}
};